{
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build"
  },
  "dependencies": {
    "jspreadsheet-ce": "^5.0.2",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.2.0"
  }
}
